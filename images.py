# import os

# all = list(
#     one
#     for one in scraper.load("meta")
#     if "metadata" in one
#     and "image" in one["metadata"]
#     and one["metadata"]["image"] is not None
# )

# os.system(f'wget {one["metadata"]["image"]}')